import styled from '@emotion/styled';

import { ReactComponent as LogoSvg } from 'common/assets/logo.svg';

export const Logo = styled(LogoSvg)`
  height: 60px;
  width: 400px;
`;

export const LogoWrapper = styled.div`
  padding: 4px 0;
`;
